#unbind C-b
#set -g prefix C-Space
#bind C-Space send-prefix

# default bindings without confirmation prompt
bind-key & kill-window
bind-key x kill-pane
#make default commands repeating
bind -r n next-window
bind -r p previous-window

set -s set-clipboard on
set -g mouse on

#bind -T copy-mode-vi v send-keys -X begin-selection
#bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel 'xclip -in -selection clipboard'

# Address vim mode switching delay (http://superuser.com/a/252717/65504)
set -s escape-time 0
set -g repeat-time 1000

# Increase scrollback buffer size from 2000 to 50000 lines
set -g history-limit 50000

# Upgrade $TERM
set -g default-terminal "screen-256color"
set -ga terminal-overrides ",xterm-256color:Tc"

# Emacs key bindings in tmux command prompt (prefix + :) are better than
# vi keys, even for vim users
#set -g status-keys emacs

set -g focus-events on

bind r source-file '/home/rgause/.tmux.conf'

setw -g mode-keys vi

# --- Alt keybinds (no prefix) ---

# Pane navigation
#bind -n M-h select-pane -L
#bind -n M-j select-pane -D
#bind -n M-k select-pane -U
#bind -n M-l select-pane -R
#
## Window navigation (0-9)
#run-shell 'for i in $(seq 0 9); do tmux bind -n M-${i} run-shell "tmux select-window -t $i || tmux new-window -t $i"; done'
#
## Window navigation (H/L)
#bind -n M-H previous-window
#bind -n M-L next-window
#
## Session navigation (J/K)
#bind -n M-J switch-client -p
#bind -n M-K switch-client -n
#
## Kill pane/window
#bind -n M-x kill-pane
#bind -n M-X kill-window
#
## Create window
#bind -n M-c new-window
#
## Rename window/session
#bind -n M-, command-prompt -I "#W" "rename-window '%%'"
#bind -n M-$ command-prompt -I "#S" "rename-session '%%'"
#
## Split panes
#bind -n M-s split-window -c "#{pane_current_path}" # horizontal
#bind -n M-v split-window -h -c "#{pane_current_path}" # vertical
#
## Resize panes with Alt + arrow keys
#bind -n M-Up resize-pane -U 1
#bind -n M-Down resize-pane -D 1
#bind -n M-Left resize-pane -L 1
#bind -n M-Right resize-pane -R 1
#
#
##bind -n M-4 new-window \; split-window -h \; split-window -v -t 1
#bind -n M-4 split-window -h -d -c "#{pane_current_path}" \; split-window -v -d -c "#{pane_current_path}" -t 2
#
#
#
## Cycle through panes in layout
#bind -n M-O select-pane -t :.+
#
## Zoom toggle
#bind -n M-z resize-pane -Z
#bind -n M-f resize-pane -Z


# --- Misc settings ---
set -g base-index 1
setw -g pane-base-index 1
